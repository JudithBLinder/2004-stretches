const decypher = (word) => {
  let objOut = {
    code: '',
    first: '',
    middle: '',
    last: '',
  };

  for (let i = 0; i < word.length; i++) {
    let char = word.charAt(i);
  }
};
module.exports = { decypher };
